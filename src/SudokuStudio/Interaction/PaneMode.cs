namespace SudokuStudio.Interaction;

/// <summary>
/// Represents a mode that describes which mode a sudoku pane is running.
/// </summary>
public enum PaneMode
{
	/// <summary>
	/// Indicates the mode is normal.
	/// </summary>
	Normal,

	/// <summary>
	/// Indicates the mode is drawing.
	/// </summary>
	Drawing
}
