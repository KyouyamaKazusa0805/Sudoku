<Page
	x:Class="Sudoku.UI.Views.Pages.SudokuPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:c="using:Sudoku.UI.Views.Controls"
	mc:Ignorable="d">
	<ScrollViewer>
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition />
			</Grid.RowDefinitions>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition />
			</Grid.ColumnDefinitions>

			<c:SudokuPane
				x:Name="_cPane" Grid.Row="0" Grid.Column="0" Margin="25,25,0,0"
				Size="560" OutsideOffset="10" />
			<InfoBar
				x:Name="_cInfoBarDetails" Title="{StaticResource SudokuPage_Details}"
				Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" IsOpen="False" />
			<Grid
				Grid.Row="1" Grid.Column="0" Margin="0,25,0,0"
				HorizontalAlignment="Center" VerticalAlignment="Center">
				<Grid.RowDefinitions>
					<RowDefinition />
					<RowDefinition />
				</Grid.RowDefinitions>
				<Grid.ColumnDefinitions>
					<ColumnDefinition />
					<ColumnDefinition />
					<ColumnDefinition />
				</Grid.ColumnDefinitions>

				<AppBarButton
					Grid.Row="0" Grid.Column="0" Icon="OpenFile" Label="{StaticResource SudokuPage_Open}"
					Click="OpenAppBarButton_ClickAsync">
					<AppBarButton.KeyboardAccelerators>
						<KeyboardAccelerator Modifiers="Control" Key="O" />
					</AppBarButton.KeyboardAccelerators>
				</AppBarButton>
				<AppBarButton
					Grid.Row="0" Grid.Column="1" Icon="Save" Label="{StaticResource SudokuPage_Save}">
					<AppBarButton.KeyboardAccelerators>
						<KeyboardAccelerator Modifiers="Control" Key="S" />
					</AppBarButton.KeyboardAccelerators>
				</AppBarButton>
				<AppBarButton
					Grid.Row="0" Grid.Column="2" Icon="Clear" Label="{StaticResource SudokuPage_Clear}">
					<AppBarButton.KeyboardAccelerators>
						<KeyboardAccelerator Modifiers="Control,Shift" Key="S" />
					</AppBarButton.KeyboardAccelerators>
				</AppBarButton>
			</Grid>
		</Grid>
	</ScrollViewer>
</Page>
