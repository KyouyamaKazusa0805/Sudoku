<Page
	x:Class="Sudoku.UI.Pages.MainWindow.SudokuPanelPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:q="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="q"
	xmlns:i="using:Sudoku.UI.Interactions"
	xmlns:c="using:Sudoku.UI.CustomControls"
	xmlns:d="using:Sudoku.UI.Drawing"
	Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
	<Page.DataContext>
		<i:SudokuPanelPageDataContext />
	</Page.DataContext>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition />
		</Grid.RowDefinitions>

		<c:SudokuPanel x:Name="SudokuPanel_Main" Grid.Row="0">
			<c:SudokuPanel.Preference>
				<d:Preference x:Name="_preference" />
			</c:SudokuPanel.Preference>
			<c:SudokuPanel.PointCalculator>
				<d:PointCalculator x:Name="_pointCalculator" ControlSize="540, 540" Offset="10" />
			</c:SudokuPanel.PointCalculator>
			<!--I don't know why I enable this, the program must throw an internal exception...-->
			<!--
			<c:SudokuPanel.GridImageGenerator>
				<d:GridImageGenerator x:Name="_gig" />
			</c:SudokuPanel.GridImageGenerator>
			-->
		</c:SudokuPanel>
		<TextBox
			x:Name="TextBox_Info" Grid.Row="1" IsReadOnly="True" Height="100"
			PlaceholderText="{StaticResource SudokuPage_TextBox_Info_PlaceholderText}" />
	</Grid>
</Page>
