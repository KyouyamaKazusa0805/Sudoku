namespace Sudoku.Graphics;

/// <summary>
/// Indicates the event handler that handles the event being triggered when the picture size is changed.
/// </summary>
/// <param name="sender">The object that triggers the event.</param>
/// <param name="e">The event data provided.</param>
public delegate void PictureSizeChangedEventHandler(GridCanvas sender, PictureSizeChangedEventArgs e);