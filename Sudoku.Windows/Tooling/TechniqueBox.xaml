<UserControl x:Class="Sudoku.Windows.Tooling.TechniqueBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sudoku.Windows.Tooling"
             mc:Ignorable="d" Width="200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <CheckBox Grid.Column="0"/>
        <TextBlock Grid.Column="1" Name="_textBlockName" Text="{Binding Self.TechniqueName,Mode=TwoWay}"/>
        <TextBlock Grid.Column="2" Name="_textBlockNamespace" Text="{Binding Self.Comment,Mode=TwoWay}"/>
    </Grid>
</UserControl>
